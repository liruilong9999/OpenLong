# OpenLong Frontend

React + Vite 仪表盘，当前包含：

- 聊天面板
- Agent 基础状态
- 实时 WebSocket 事件提示
- Memory 仪表盘（读取后端 `/dashboard/memory/{agent_id}`）

## 启动

```bash
npm install
npm run dev
```

也可以直接在仓库根目录运行：

```bash
python start.py --frontend-only --frontend-command dev
```

function MemoryView() {
  return (
    <section className="card">
      <h2>Memory</h2>
      <p className="hint">
        Memory explorer is intentionally left as a scaffold placeholder.
      </p>
      <p className="hint">
        Backend already writes logs under each agent workspace: memory/logs and memory/summaries.
      </p>
    </section>
  );
}

export default MemoryView;
